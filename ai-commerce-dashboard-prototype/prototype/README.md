# Static Prototype Archive

The root-level `index.html`, `styles.css`, and `app.js` are the preserved static prototype used to define the current product frame:

- Commerce operations
- Media operations
- System settings

Do not mutate those files during M0. The production frontend scaffold lives in `apps/web`.

